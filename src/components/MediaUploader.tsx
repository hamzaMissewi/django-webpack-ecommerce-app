import React from 'react';
import {TextField} from "@mui/material";


function MediaUploader() {

    return (
        <div>
            Upload file
            {/*<TextField type={"file"} name={""} onChange={handleChangeFile}/>*/}
        </div>
    );
}

export default MediaUploader;