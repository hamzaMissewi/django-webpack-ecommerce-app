export interface LoaderProps {
    size?: number;
    primaryColor?: string;
    secondaryColor?: string;
    duration?: number;
}

export interface ProductProps {
    title: string,
    description: string,
    url: string,
    price: number;
}